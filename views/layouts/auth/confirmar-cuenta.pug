extends ../layouts/index

block contenido
    div.py-10
        h1.text-4xl.my-10.font-extrabold.text-center Bienes
            span.font-normal Raices
        h2.text-center.texto-2xl.font-extrabold=pagina

    div( class="max-w-md px-4 py-8 mx-auto bg-white rounded-lg shadow")
        p( class= error ? 'bg-red-500' : 'bg-green-500').py-2.px-5.rounded-lg.w-full.font-bold.text-center.uppercase.text-white=mensaje

        if !error
            a( href="/auth/login" class="block mt-10 text-sm font-bold text-center uppercase text-slate-600") Ya puedes iniciar sesion