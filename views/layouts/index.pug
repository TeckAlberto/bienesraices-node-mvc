doctype html
html(lang="es")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Buenes Raices | #{pagina} 
        link(rel="stylesheet", href="/css/app.css")
    body.min-h-screen.bg-gray-50
        if barra
            include header

        main(class="container px-2 mx-auto mt-10")
            block contenido

        block scripts